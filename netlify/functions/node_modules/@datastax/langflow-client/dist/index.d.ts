import { fetch } from "undici";
import { Flow } from "./flow.js";
import type { LangflowClientOptions, RequestOptions, StreamEvent, Tweaks } from "./types.js";
export declare class LangflowClient {
    #private;
    baseUrl: string;
    basePath: string;
    langflowId?: string;
    apiKey?: string;
    fetch: typeof fetch;
    defaultHeaders: Headers;
    constructor(opts: LangflowClientOptions);
    flow(flowId: string, tweaks?: Tweaks): Flow;
    request(options: RequestOptions): Promise<unknown>;
    stream(options: RequestOptions): Promise<ReadableStream<StreamEvent>>;
}
